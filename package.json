{
  "name": "prolog-program-mithril",
  "version": "0.2.0",
  "description": "Program viewer for Prolog.",
  "main": "program.compiled.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/main.js --bundle --minify --outfile=program.compiled.js --tree-shaking=true --target=chrome58,firefox57,safari11,edge16",
    "watch": "esbuild src/main.js --bundle --watch --outfile=program.compiled.js"
  },
  "author": "Andreas Ekeroot",
  "license": "Apache-2.0",
  "dependencies": {
    "cookie_js": "^1.3.0",
    "mithril": "^2.2.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.53"
  }
}
